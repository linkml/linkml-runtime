[tox]
envlist = py39, py310, py311, py312, py313

[testenv:py]
skip_install = true
allowlist_externals = poetry
commands_pre =
    poetry install
commands =
    poetry run pytest

[testenv:lint]
skip_install = true
deps =
    ruff
    black
    codespell
commands =
    ruff check
    black --check --diff .
    codespell

[testenv:format]
skip_install = true
deps =
    ruff
    black
    codespell
commands =
    ruff check --fix
    black .
    codespell
